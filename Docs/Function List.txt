List of available funtions per package:

1) Wave Mechanics
	a. wavelength(T,d)
		Input:
		T (s), wave period.
		d (m), water depth.
		Output:
		L = Wave Length (m)
	b. relativedepth(d,L)
		Input:
		d (m), water depth.
		L (m), wave length.
		Output:
		string = relative water depth.
	c.
